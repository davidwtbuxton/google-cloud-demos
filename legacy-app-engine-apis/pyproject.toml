[project]
name = "legacy-app-engine-apis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "appengine-python-standard",
    "google-cloud-logging",
    "google-cloud-storage",
    "Flask",
    "Flask-Caching",
]
