runtime: python39
app_engine_apis: true
env_variables:
  DJANGO_SETTINGS_MODULE: "demo.settings.production"

handlers:

  - url: /static
    static_dir: static
    secure: always

  - url: /.*
    script: auto
    secure: always
